version: "1"
rules:                           # Array of rules
  - base: develop                 # Required. Target branch
    upstream: master         # Required. Must be in the same fork network.
    autoMerge: true              # Optional, Default: false
    autoMergeHardReset: false     # Optional, Default: false DANGEROUS Wipes target branch changes and reset ref to match upstream
#  - base: dev
#    upstream: master
#    assignees:                   # Optional
#      - wei
#    reviewers:                   # Optional
#      - wei
label: ":arrow_heading_down: pull"       # Optional
